# Piano di lavoro — 2026-09-05

Stato di partenza: `main` allineato a `origin/main` (`292a407`). La coda UI
dell'audit 2026-09-01 è per metà chiusa (task 011-016 del runner autonomo,
S8/UI-01, UI-05, UI-15, UI-17/18). Il runner notturno è di nuovo operativo
(ha girato stanotte, sei task, tutti verdi).

Questo piano ordina ciò che resta per **chi può farlo**: il runner da solo,
io con te in sessione, o tu con una decisione editoriale.

---

## A. Decisioni che bloccano il resto (servi tu)

Queste tre domande sono aperte da giorni e bloccano lavoro già preparato.

### A1. Collazione su Lane: si fa o no? — **la più importante**
La collazione a campione del 2026-09-01 ha trovato **8 schede divergenti su 12**
(non solo markup: letture sbagliate, ILA-026 `σγζ΄` per `σ̣ν̣ζ΄`, ILA-132 la multa,
ILA-103 tre letture). Se il tasso regge sul corpus intero, ~200 schede su 295 hanno
un errore di trascrizione. Tutto il lavoro di markup (F3, F4, lessico cultuale) si
appoggia su un testo di cui non ci fidiamo.

**Proposta:** non decidere al buio. Faccio una **collazione stratificata su 30 schede**
(≈10 %, estratte per regione e per lunghezza del testo) sul PDF di Lane, con un
rapporto che dà tasso e tipologia degli errori. Costo: una sessione. Poi decidi se
la collazione totale vale la pena o se basta correggere le classi di errore ricorrenti.

### A2. Quale repo dati è canonica
Il clone locale di `Gregoee2002/ILA` è fermo al 2026-07-26, con 50 file `CMRDM-*`;
`src/data/corpus/` ne ha 295 con naming `ILA-NNN`. Finché non è chiaro **quale vince**
e se `DATA_REPO_TOKEN` è nei secret di Actions, ogni fix XML rischia di essere
annullata dal boot-sync. Va risolto prima di A3 e di tutto il blocco B.

### A3. D2 — `placeName type="ethnic"` o `rs type="ethnic"`?
Una riga di decisione che sblocca F4 (13 etnici + toponimi). Nessun'altra dipendenza.

---

## B. Corpus e markup (io in sessione, dopo A2)

| # | Lavoro | Stato | Blocco |
|---|---|---|---|
| B1 | **F3** — `<num>`/`<date>`: 53 schede con numerali, checksum astragali ILA-120–124, 3 numerali irregolari in apparato | criteri già scritti, pronto | A2 |
| B2 | **F4** — 13 etnici + toponimi | pronto | A2 + A3 |
| B3 | **Lessico cultuale**: applicare il tagging v1 al corpus (330 righe di spoglio, tassonomia a 5 famiglie, renderer viola già in entrambi i renderer) | fermo su branch, mai su main | A2 |
| B4 | **Traduzioni rev. 2**: 213 schede riviste in `docs/traduzioni-brevi-rev2.json`, mai riversate nel corpus | pronto | A2 |
| B5 | **DATA-01** epiteti: `Dionysos` è un teonimo; patronimici in genitivo (`Artemidorou …`, `Diodotou`) fuori dagli epiteti; `Tarsene`/`Tarsios` e `Axetenos` da verificare su Lane | serve verifica su Lane | A1 |
| B6 | **DATA-02** granularità teonimi: applicare in modo uniforme "teonimo puro + epiteto separato" (`Men` + `Tyrannos`) | serve la decisione d'impianto | — |
| B7 | **DATA-03** ILA-294: patch pronta in `docs/patches/ILA-294.xml.fixed` | pronto, 5 minuti | A2 |

B4 e B7 sono i due lavori a rapporto valore/costo più alto qui: entrambi pronti,
entrambi fermi solo per A2.

---

## C. UI — coda dell'audit (il runner, da solo)

Già in `tasks.yaml`, pronti a girare stanotte:
- **017** primitivo `<Button>` (+ uniformare `disabled:opacity-40/-50`)
- **018** `<Chip>` / `<Badge>`
- **019** `<FilterSelect>` per i 7 filtri di `App.tsx`
- **020** `<Card>` glassmorphism

Da aggiungere a `tasks.yaml` (S11 e code non ancora tradotte in task):
- **021** tipografia: `fontFamily` inline → token; scala `--text-*` al posto di
  `text-2xl`/`text-[9px]` sparsi; `.field-label` unico primitivo
- **022** transizioni: da 4 durate (`150/200/300/500`) a 2 (micro / pannelli)
- **023** UI-18: ramo vuoto/loading con `role="status"` in `RegistroPanel`,
  `DraftReviewPanel`, `BugReportsPanel`, `IconographyPanel`
- **024** z-index inline nei componenti React → scala `--z-*` introdotta dal task 016

**Verifica a vista che resta a carico nostro** (il codemod ha normalizzato le opacità,
es. `bg-amber-400/5` → `bg-warning/10`, quindi alcuni fondi sono più carichi):
chip di salvataggio, banner d'errore, badge revisione, tooltip mappa. Più due punti
segnalati dall'agente: l'hover del bottone di login in `SectionEditorView.tsx`
(ora `hover:opacity-70`) e l'hover rosso su "Annulla" del form vocabolario —
annullare non è eliminare, la scelta è aperta.

---

## D. Infrastruttura

- **D1.** Il PAT non ha scope `workflow`: il gate `tsc` in `deploy-pages.yml` resta in
  `docs/patches/S1-deploy-pages-tsc-gate.patch` e il task 010 (CI con typecheck +
  well-formedness XML) non è pushabile. Rigenerare il token con lo scope giusto è
  un minuto di lavoro tuo e sblocca due cose.
- **D2.** Bundle `index-*.js` da 1,60 MB minificati (446 KB gzip): code splitting su
  mappa/heatmap/editor. Vale una sessione a sé.
- **D3.** Pulizia: i branch `auto/task-011..016` sono mergiati e si possono cancellare.
- **D4.** Bibliografia: la forma canonica delle citazioni Lane è ancora indecisa
  (`docs/norme-bibliografia.md`). Fino ad allora il pannello bulk-edit resta a metà.

---

## Ordine consigliato

1. Rispondi ad **A2** e **A3** (due righe) e rigenera il token di **D1**.
2. Io faccio **B7 + B4** — pronti, a basso rischio, valore immediato sul sito.
3. Io faccio la **collazione a campione di A1** e ti porto il tasso d'errore:
   è la decisione che orienta i prossimi mesi.
4. Il runner intanto smaltisce **017-020**, poi i nuovi **021-024**.
5. Con la risposta di A1: o collazione totale, o si va avanti con **B1/B2/B3**.
